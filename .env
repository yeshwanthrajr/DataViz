# Storage Configuration
# Choose your storage type: memory, json, mysql, postgres
STORAGE_TYPE="json"

# JSON Storage Configuration (default)
# Data will be stored in ./data.json file
# No additional setup required!

# MySQL Configuration (if using MySQL)
DB_HOST="localhost"
DB_USER="root"
DB_PASSWORD=""
DB_NAME="fileflowpro"

# PostgreSQL Configuration (if using postgres)
# For local PostgreSQL (if you have it installed):
# DATABASE_URL="postgresql://postgres:password@localhost:5432/fileflowpro"

# For Neon Database (recommended - free and easy to set up):
# 1. Go to https://neon.tech
# 2. Create a free account
# 3. Create a new project
# 4. Copy the connection string and replace the placeholder below
DATABASE_URL="postgresql://your-neon-connection-string-here"

# JWT Secret
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Server Configuration
NODE_ENV="development"
PORT=5000